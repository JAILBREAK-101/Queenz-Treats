import React from 'react';
import Loader from 'react-spinners-components';

// For Auth Buttons

const Spinner = () => {
    return (
      <div>
          <Loader color="#00BFFF" />
      </div>
    );
  }
  
  export default Spinner;